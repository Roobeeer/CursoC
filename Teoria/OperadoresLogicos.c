#include <stdio.h>
#include <stdbool.h>
int main() {
    //AND && comprueba si dos o más condiciones son ciertas

    float temperature = -1000;

    /*if(temperature >=0 && temperature <= 60) {
        printf("The weather is good");
    }else {
        printf("The weather is bad");
    }*/


    //OR || checkea si alguna de las condiciones es correcta
    /*if(temperature <=0 || temperature >= 60) {
        printf("The weather is bad");
    }else {
        printf("The weather is good");
    }
*/
    //NOT ! niega

   /* bool sunny = false;
    if(!sunny == false) {
        printf("The weather is bad ");
    }else {
        printf("The weather is good");
    }
    */





    return 0;
}